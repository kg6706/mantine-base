'use client';

import { createTheme } from '@mantine/core';

export const theme = createTheme({
  // components: {
  //   Input: {
  //     styles: (theme: MantineTheme, props: InputProps) => ({}),
  //   },
  // },
});
